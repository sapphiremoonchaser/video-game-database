[project]
name="video_games_database"
version="1.0.0.0"
requires-python="<=3.12.0"
dependencies =[
#    "requests",
#    "pandas",
#    "sqlalchemy",
#    "python-dotenv"
]

[pyprojct.optional-dependencies]
dev = [
    #pytest
]

[build-system]
requires = [
    "setuptools<=6.1.0",
    "wheel>=0.45.1"
]

build-backend="setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]